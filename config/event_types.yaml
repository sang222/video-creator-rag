catalog_key: event_types
catalog_version: "1.2.0"
schema_version: catalog.v1
status: active
items:
  - event_type: audit.event_recorded
    event_version: 1
    description: Audit event persisted.
  - event_type: domain.event_recorded
    event_version: 1
    description: Domain event persisted to local outbox.
  - event_type: config.catalog_seeded
    event_version: 1
    description: Config catalog version persisted.
  - event_type: video_project.created
    event_version: 1
    description: Video project created with explicit policy snapshot.
  - event_type: artifact.created
    event_version: 1
    description: Workflow artifact created.
  - event_type: artifact_version.created
    event_version: 1
    description: Immutable artifact version created.
  - event_type: review_task.created
    event_version: 1
    description: Review task created for an exact target.
  - event_type: review_finding.created
    event_version: 1
    description: Review finding recorded.
  - event_type: revision_request.created
    event_version: 1
    description: Revision request created for an exact artifact version.
  - event_type: revision_request.resolved
    event_version: 1
    description: Revision request resolved by a newer artifact version.
  - event_type: approval_decision.created
    event_version: 1
    description: Approval decision recorded for an exact target.
  - event_type: approval_decision.rejected_or_blocked
    event_version: 1
    description: Approval decision was rejected or blocked before approval.
  - event_type: gate_definition_version.created
    event_version: 1
    description: Gate definition version created.
  - event_type: gate_definition_version.activated
    event_version: 1
    description: Gate definition version activated.
  - event_type: gate_run.created
    event_version: 1
    description: Deterministic gate run created.
  - event_type: gate_run.review_task_created
    event_version: 1
    description: Gate run created a linked review task.
  - event_type: platform_policy_catalog.created
    event_version: 1
    description: Platform policy catalog created.
  - event_type: platform_policy_version.created
    event_version: 1
    description: Platform policy version created.
  - event_type: platform_policy_version.activated
    event_version: 1
    description: Platform policy version activated.
  - event_type: policy_source_ref.created
    event_version: 1
    description: Policy source reference created.
  - event_type: policy_change_record.created
    event_version: 1
    description: Policy change record created.
  - event_type: policy_change_record.state_changed
    event_version: 1
    description: Policy change record state changed.
  - event_type: policy_revalidation_batch.created
    event_version: 1
    description: Policy revalidation batch created.
  - event_type: policy_revalidation_batch.completed
    event_version: 1
    description: Policy revalidation batch completed.
